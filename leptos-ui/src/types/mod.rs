//! Domain types matching the TypeScript `types.ts` and API module types.
//!
//! All types use `serde` for JSON serialization/deserialization
//! and are compatible with the backend API responses.

pub mod api;
pub mod core;
pub mod events;
